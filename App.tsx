import React from "react";
import "./App.css";

export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-slate-50">
      <div className="max-w-2xl p-8 bg-white rounded-2xl shadow">
        <h1 className="text-2xl font-semibold text-blue-600 mb-4">🎓 BTech Feedback App</h1>
        <p className="text-slate-700">Starter project — run <code>npm install</code> and <code>npm run dev</code>.</p>
      </div>
    </div>
  );
}
